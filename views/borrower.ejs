      <!DOCTYPE html>
      <html lang="en">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <title>Borrower Dashboard - Borderless Yield</title>
        <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
      </head>
      <body class="bg-gradient-to-br from-blue-50 to-green-100 min-h-screen text-gray-800">

        <nav class="bg-white shadow p-4 flex justify-between items-center">
          <h1 class="text-xl font-bold text-green-700">Borderless Yield</h1>
          <a href="/investor" class="text-blue-600 hover:underline">Ready to Invest</a>
        </nav>

        <main class="max-w-4xl mx-auto p-6 mt-10">
          <h2 class="text-3xl font-bold text-center text-green-800 mb-6">Apply for a Microloan</h2>

          <!-- Investment Meter -->
          <div class="bg-white rounded-xl shadow p-6 mb-6">
            <h3 class="text-xl font-semibold text-gray-700 mb-2">Your Investment Meter</h3>
            <div class="w-full bg-gray-200 rounded-full h-4">
              <div class="bg-green-500 h-4 rounded-full" style="width: 60%"></div> <!-- Change this based on logic -->
            </div>
            <p class="text-sm text-gray-600 mt-2">You’ve invested <strong>2</strong> times. Invest 1 more time to unlock loan eligibility.</p>
          </div>

          <!-- Application Form -->
          <form action="/apply-loan" method="POST" class="bg-white rounded-xl shadow p-6 space-y-4">
            <h3 class="text-xl font-semibold text-gray-700 mb-4">Loan Application</h3>

            <input type="text" name="name" placeholder="Full Name" required class="w-full border p-3 rounded" />
            <input type="text" name="country" placeholder="Country" required class="w-full border p-3 rounded" />
            <select name="stablecoin" required class="w-full border p-3 rounded">
              <option value="">Select Stablecoin</option>
              <option value="cKES">cKES (Kenya)</option>
              <option value="cCOP">cCOP (Colombia)</option>
              <option value="cGHS">cGHS (Ghana)</option>
            </select>
            <input type="number" name="amount" placeholder="Amount Requested (USD)" required class="w-full border p-3 rounded" />
            <textarea name="reason" placeholder="Why do you need the loan?" rows="4" required class="w-full border p-3 rounded"></textarea>
            <button type="submit" class="bg-green-600 text-white px-6 py-3 rounded-full font-semibold hover:bg-green-700 transition">
              Submit Application
            </button>
          </form>

        <footer class="text-center mt-10 text-gray-500">
          Borderless Yield © <%= new Date().getFullYear() %>
        </footer>
      </body>
      </html>      
      